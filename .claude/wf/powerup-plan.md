# Implementation Plan (REVISED r3): Power-Up Keyword Mechanic (MSH/MSC)

> Round-2 review APPROVED almost everything. This r3 revision applies five TARGETED fixes and leaves all confirmed-correct content unchanged:
> - **FIX 1 (B1 redesign):** replace the invented stateful-latch `RestrictionExpiry::EndOfPlayerNextTurn { player, armed }` with the EXISTING convert-to-`EndOfTurn` arming idiom. New leaf is `RestrictionExpiry::UntilEndOfNextTurnOf { player }` (mirrors `Duration::UntilEndOfNextTurnOf`), pre-armed at lowering, converted to the existing `RestrictionExpiry::EndOfTurn` at the target player's untap step (mirror `prune_until_next_turn_effects`). Reuses the existing cleanup prune (zero new cleanup-prune code, no `bool` field).
> - **FIX 2 (B2 site count):** there are **SEVEN** `ActivateAbilities {` sites, not six. The missed 7th is a `matches!(...)` test assertion in `parser/oracle_effect/mod.rs:42657` with no `..` inside the braces.
> - **FIX 3 (skip-leak edge):** document the narrow skipped/prevented-extra-turn arming-leak edge in Kang.
> - **FIX 4 (M4 reframing):** the real work is threading `ability_tag` through the `pay_ability_*` signature family (~8 call sites), not "6 `PaymentContext::Activation` literal sites."
> - **FIX 5 (three minors):** Exhaust (not Boast) is the parser template; Hulk's new `ReduceAbilityCost` arm uses the clean `tag`/`take_until` combinator style (not `split_once_on`/`scan_contains`); `CR 514` → `CR 514.2`.
>
> Everything else is preserved from the round-2-approved plan: B2 design (only_tag + the `casting.rs:469` consult site already has `AbilityDefinition` in scope), M1 (per-game limit path), M2 (40-card class), M3 (SelfManaValue printed-CMC reduction), M5 (Quinjet two abilities), the `AbilityTag::PowerUp` leaf, `ManaRestriction::OnlyForTaggedActivation`, `ModifyActivationLimit`, Marvel Boy dual-trigger, all CR numbers. Every code site below was read against live source; line numbers are current as of this revision.

---

## 0. Executive Summary & Headline Finding

**Power-up is mostly pre-built infrastructure, but it requires three genuinely new pieces of infrastructure (not reuse): a new `RestrictionExpiry` temporal anchor for Kang (B1), a parameterization of `ProhibitedActivity::ActivateAbilities` to scope the prohibition to a tag (B2), and threading the activating ability's tag through the cost-payment chain into `PaymentContext::Activation` for Quinjet (M4/M5).** The engine already has an `AbilityTag`-based labeled-activated-ability subsystem (Boast/Outlast/Cycling/Exhaust/Evolve/Backup) with per-tag activation-limit statics, per-tag activation-cost-reduction statics, per-tag/per-source per-game and per-turn activation counters, a tag-carrying activation event, a tag-keyed trigger, and a self-referential `CostReduction` struct. Combined with the **already-existing** `QuantityRef::SelfManaValue` and `ParsedCondition::SourceEnteredThisTurn`, the dynamic "reduce the cost by his mana cost if he entered this turn" reduction is expressible **with zero new quantity/condition types**.

The plan adds:
1. **One new `AbilityTag::PowerUp` variant** (leaf; correct).
2. **One new `ManaRestriction::OnlyForTaggedActivation(AbilityTag)` variant** + threading the activating ability's tag through the cost-payment chain to the spend gate (M4/M5).
3. **A parameterization of `ProhibitedActivity::ActivateAbilities`** with `only_tag: Option<AbilityTag>` (B2).
4. **A new `RestrictionExpiry::UntilEndOfNextTurnOf { player }` variant** (B1) — pre-armed at lowering, converted to the existing `RestrictionExpiry::EndOfTurn` at the target player's untap step (mirroring the existing `Duration::UntilEndOfNextTurnOf` arming idiom in `layers.rs::prune_until_next_turn_effects`); the existing cleanup-step prune then ends it. **No new cleanup-prune code, no `bool` latch field.**
5. **A per-game limit path** for Wonder Man (M1), sharing only the static-scan with the existing per-turn path.
6. **Parser dispatch** for the `Power-up —` line + four static/restriction/trigger phrases (all nom).

Honestly deferred: **Advancing the Spirit** (per-turn alternative-cost replacement keyed on power-up abilities) — out of scope; remains an unsupported coverage gap.

---

## 1. The Power-Up Card Class (grounded; `client/public/card-data.json`, 40 cards total)

`grep`-verified count: **40 cards** carry power-up text. Class breakdown:

| Bucket | Count | Cards | Coverage |
|---|---|---|---|
| **Base `Power-up —` activated line** | **37** | All cards with a `Power-up — {cost}: …` ability (Nick Fury, Jack of Hearts, Hulk, Wonder Man, Kang, Thanos, Goliath, Immortus, Pet Avengers, Viv Vision, etc.) | Covered by the base primitive (§3 Steps 1-3) once their sub-effects are independently supported |
| **Static cost-reduction (class-scoped)** | 1 | Hulk, Gamma Goliath (`Power-up abilities of other creatures you control cost {3} less to activate`) | §3 Step 4 |
| **Static additional-activation** | 1 | Wonder Man, Hollywood Hero (`Each power-up ability of permanents you control can be activated an additional time`) | §3 Step 5 (M1) |
| **Tag-scoped mana restriction** | 1 | Quinjet Technician (`{T}: Add {R}{R}. Spend this mana only to activate power-up abilities.`) | §3 Step 6 (M4/M5) |
| **Extra-turn prohibition** | 1 | Kang the Conqueror (`Take an extra turn after this one. During that turn, power-up abilities can't be activated.`) | §3 Step 7 (B1/B2) |
| **Trigger on power-up activation** | 1 | Marvel Boy, Noh-Varr (`whenever you activate a power-up ability, …`) | §3 Step 8 |
| **DEFERRED — per-turn alt-cost replacement** | 1 | **Advancing the Spirit** (`pay {0} rather than pay the power-up cost of the first power-up ability you activate during each of your turns`) | **Out of scope (§2.10).** Remains unsupported / coverage-red. |

Note: the printed reminder text uses possessive variants ("his / her / its / she") across cards — irrelevant to parsing because the cost reduction is set from the keyword definition, not parsed from the reminder (`strip_reminder_text` removes the parenthetical entirely; `oracle_util.rs:242`).

**Verified card texts (live data):**
- **Nick Fury, Agent of S.H.I.E.L.D.**: `Power-up — {W}{U}{B}{R}{G}: Put two +1/+1 counters on Nick Fury, then look at the top seven cards of your library. You may put a Hero, Equipment, or Vehicle card from among them onto the battlefield. If it's a double-faced card, you may transform it. Put the rest on the bottom of your library in a random order.`
- **Jack of Hearts, Volatile Hero**: `Power-up — {4}{R}{R}: Discard your hand, then draw three cards. Put two +1/+1 counters on Jack of Hearts.`
- **Hulk, Gamma Goliath**: `Reach, trample` / `Power-up abilities of other creatures you control cost {3} less to activate.` / `Power-up — {6}{R}{G}: Put five +1/+1 counters on Hulk.`
- **Wonder Man, Hollywood Hero**: `Flying` / `Each power-up ability of permanents you control can be activated an additional time.` / `Power-up — {5}{R}{R}: Put two +1/+1 counters on Wonder Man.`
- **Quinjet Technician**: `{T}: Add {R}.` / `{T}: Add {R}{R}. Spend this mana only to activate power-up abilities.`
- **Kang the Conqueror**: `Flying` / `Power-up — {5}{U}{U}{U}: Put a +1/+1 counter on Kang. Take an extra turn after this one. During that turn, power-up abilities can't be activated.`
- **Marvel Boy, Noh-Varr**: `Whenever another creature you control enters and whenever you activate a power-up ability, put a +1/+1 counter on Marvel Boy.`

---

## 2. Mandatory Architectural Sections

### 2.1 Pattern Coverage
Power-up is a **keyword-labeled activated-ability class** — the same architectural shape as Boast/Outlast/Cycling/Exhaust. The base-case primitive (tagged activated ability with per-game limit + self-MV cost reduction) **directly covers 37 cards**. The six named cards exercise independent reusable axes (class-scoped static cost reduction, class-scoped activation-limit static, tag-scoped mana restriction, tag-scoped activation prohibition, tag-scoped trigger), each generalizing to **any future labeled-ability mechanic** and hardening the shared `AbilityTag` subsystem. No axis is a one-card special case. **Distinct surfaces: 37 base-case + 6 named (Hulk/Wonder Man/Kang each also appear in the base bucket, so the union of named+base is ~37 cards); 1 deferred outlier (Advancing the Spirit).** The prior plan's claim that this "unlocks the entire power-up cycle" is **removed** — Advancing the Spirit's per-turn alt-cost replacement is a real, honest gap.

### 2.2 Building Blocks (compose, do not recreate)

| Need | Existing building block | File:line (verified) |
|---|---|---|
| Label an activated ability | `AbilityDefinition.ability_tag: Option<AbilityTag>` + `AbilityTag` enum | `types/ability.rs:11804` (field), `:11627-11645` (enum) |
| Per-game activation limit ("only once") | `ActivationRestriction::OnlyOnce` + `activated_abilities_this_game` keyed `(source_id, ability_index)` | restriction enum `ability.rs:11649`; consult `restrictions.rs:725-732`; counter `game_state.rs:5799-5805`; increment `restrictions.rs:555-556` |
| Dynamic cost reduction by source MV, gated entered-this-turn | `CostReduction { amount_per, count, condition }` + `QuantityExpr::Ref { qty: QuantityRef::SelfManaValue }` + `ParsedCondition::SourceEnteredThisTurn` | struct `ability.rs:11731-11745`; `SelfManaValue` resolution `quantity.rs:1663-1675`; condition `ability.rs:5294` |
| Apply dynamic reduction at activation (generic-only) | `apply_cost_reduction` → `reduce_generic_in_cost` | `casting.rs:12507-12530` (generic reduce `:12526`) |
| Class-scoped static cost reduction (Hulk) | `StaticMode::ReduceAbilityCost { keyword, amount, minimum_mana, dynamic_count }` honoring `affected` + `minimum_mana` | enum `statics.rs:800`; runtime `apply_static_activated_ability_cost_reduction` `casting.rs:12535-12569` |
| Class-scoped activation-limit raise (Wonder Man) | `StaticMode::ModifyActivationLimit { keyword, new_limit }` + the static-scan inside `effective_activation_limit` | enum `statics.rs:815`; scan `restrictions.rs:590-621` |
| Tag-scoped mana spend restriction (Quinjet) | `ManaRestriction` enum + `allows_activation`/`allows` + `PaymentContext::Activation` | enum `mana.rs:298-360`; `allows_activation` `mana.rs:471-507` (keyed on source types); `allows` `mana.rs:513-523`; ctx `mana.rs:268-281` |
| Extra-turn machinery (Kang) | `state.extra_turns: Vec<PlayerId>` (LIFO) + `start_next_turn` pop | `game_state.rs:5664-5667`; `turns.rs:435-466` |
| Activation prohibition (Kang) | `GameRestriction::ProhibitActivity { source, affected_players, expiry, activity }` + `Effect::AddRestriction` + `fill_runtime_fields` | enum `ability.rs:1560-1565`; consult `is_blocked_by_cant_activate_abilities` `casting.rs:460-492`; lowering `add_restriction.rs:30-106` |
| End-of-next-turn arming idiom (Kang B1) | `Duration::UntilEndOfNextTurnOf` convert-to-`UntilEndOfTurn` at the grantee's untap step → existing cleanup prune ends it | doc `ability.rs:1512-1521`; arming `layers.rs:409-426` (`prune_until_next_turn_effects`) + casting-permission arming `layers.rs:252-294`; called at untap `turns.rs:724` |
| Tag-scoped trigger (Marvel Boy) | `TriggerMode::KeywordAbilityActivated(AbilityTag)` + `GameEvent::KeywordAbilityActivated { ability_tag, .. }` + tag-agnostic emit + matcher | trigger `triggers.rs:385`; event `events.rs:753-758`; emit `casting_targets.rs:657-695`; matcher `trigger_matchers.rs:3918-3944` |
| Dual-condition trigger split (Marvel Boy) | `split_and_when_compound` | `oracle_trigger.rs:4656` |
| Parser template for `Power-up —` line | **Exhaust em-dash parser block** (already pushes `OnlyOnce` + sets `ability_tag` — exactly power-up's needs) | `oracle.rs:2428-2455` (`nom_on_lower` + `value`/`alt`/`tag` + `find_activated_colon` + `parse_oracle_cost` + `parse_effect_chain_with_context`) |
| Reminder-text stripping | `strip_reminder_text` | `oracle_util.rs:242` |
| Mana-restriction lowering | `resolve_restrictions` (`ManaSpendRestriction` → `ManaRestriction`) | `effects/mana.rs:257-316` |
| AI/frontend activation surface | `GameAction::ActivateAbility { source_id, ability_index }` (generic, already wired) | `types/actions.rs`, `ai_support/candidates.rs` |

**Justified new helpers (only these):**
1. `AbilityTag::keyword_str(self) -> &'static str` — promote the inlined tag→keyword `match` in `effective_activation_limit` (`restrictions.rs:577-589`) to a method so the per-turn path, the new per-game path, AND the static-cost-reduction gate share one authority. (Single point of truth; mirrors how other enums expose canonical string keys.)
2. `activation_limit_from_statics(state, player, source_id, keyword: &str) -> u32` — the shared `ModifyActivationLimit` static-scan extracted from `effective_activation_limit` so both per-turn and per-game limit functions call it (M1).
3. `effective_activation_limit_per_game(state, player, source_id, ability_index) -> u32` — per-game wrapper (base 1, calls `activation_limit_from_statics`). (M1.)
4. New tag-scoped match arms in `allows_activation`/`allows`, the new `RestrictionExpiry::UntilEndOfNextTurnOf` untap-conversion arm, the `only_tag` consult — not standalone helpers, but new arms/branches in existing functions.

### 2.3 Logic Placement
- **Parser** (`oracle.rs`, `oracle_static/dispatch.rs`, `oracle_effect/mana.rs`, `oracle_trigger.rs`, `oracle_effect/mod.rs`): detect the `Power-up —` line; the two static phrases (Hulk, Wonder Man); the Quinjet mana phrase; the Kang prohibition clause; the Marvel Boy power-up trigger. Parser only constructs AST.
- **Types** (`types/ability.rs`, `types/mana.rs`, `types/statics.rs`): add `AbilityTag::PowerUp`; add `ManaRestriction::OnlyForTaggedActivation(AbilityTag)` (+ parser-level `ManaSpendRestriction::ActivateTagged`); add `only_tag` to `ProhibitedActivity::ActivateAbilities`; add `RestrictionExpiry::UntilEndOfNextTurnOf { player }`; add `ability_tag` to `PaymentContext::Activation`.
- **Engine** (`restrictions.rs`, `casting.rs`, `costs.rs`, `casting_costs.rs`, `casting_targets.rs`, `engine.rs`, `mana_abilities.rs`, `turns.rs`, `add_restriction.rs`, `effects/mana.rs`): wire the tag→keyword method; make the `OnlyOnce` arm per-game-limit-aware; generalize the static-cost-reduction keyword gate; thread the activating tag through the cost-payment chain into `PaymentContext::Activation`; consult `only_tag` at the prohibition site; arm (convert) the new `RestrictionExpiry` variant at the untap step. All game rules in the engine.
- **Frontend/AI**: zero new surface — power-up abilities are ordinary indexed activated abilities flowing through `ActivateAbility`.

### 2.4 Rust Idioms
- Extend `AbilityTag` (one variant) — not a `bool is_power_up`. The tag is the composable axis (no-bool rule).
- The dynamic cost reduction reuses typed `QuantityExpr::Ref(QuantityRef::SelfManaValue)` + `ParsedCondition::SourceEnteredThisTurn` — no stringly-typed or new leaf types.
- Exhaustive `match` over `AbilityTag` in `effective_activation_limit`'s keyword `match` (no wildcard — `restrictions.rs:577-589`) forces the compiler to flag the new arm.
- `ProhibitedActivity::ActivateAbilities.only_tag` is `Option<AbilityTag>` (typed, additive, `None` preserves existing "all activations" behavior) — not a parallel bool or sibling variant.
- The new `RestrictionExpiry::UntilEndOfNextTurnOf { player }` carries the typed `PlayerId` and **mirrors the existing `Duration::UntilEndOfNextTurnOf` name and arming idiom** — no `bool` latch (the convert-at-untap pattern provides the temporal arming for free). Matched exhaustively at every prune site.
- Quinjet's restriction is a `ManaRestriction` leaf keyed on `AbilityTag`, not on the source permanent's type.

### 2.5 Nom Compliance (mandatory — parser files change)
Every detection is a nom combinator; **no `contains()`/`starts_with()`/`find()`/`.split(`/`.rfind(` for dispatch.** The existing static parsers in `oracle_static/dispatch.rs` use `nom_primitives::scan_contains` only as a cheap pre-filter and `take_until`/`tag` for the real parse — new arms follow the clean `tag`/`take_until`/`delimited` composition (the `dispatch.rs:2186` style, NOT the `dispatch.rs:2213` `split_once_on`/`scan_contains` style, which are parser-gate blind spots).
- **`Power-up —` line** (`oracle.rs`, modeled on the **Exhaust** block `:2428-2455`): `nom_on_lower(&line, &lower, |i| value((), alt((tag("power-up \u{2014} "), tag("power-up -- ")))).parse(i))`, then `find_activated_colon`, `parse_oracle_cost`, `parse_effect_chain_with_context(&effect_text, AbilityKind::Activated, &mut ctx)`. (Exhaust is the template because it already pushes `ActivationRestriction::OnlyOnce` and sets `ability_tag` — exactly power-up's needs. Boast is the WRONG template: it uses `OnlyOnceEachTurn` + a `RequiresCondition(SourceAttackedThisTurn)` condition.)
- **Hulk static** (`oracle_static/dispatch.rs`, NEW `alt` arm near the existing `ReduceAbilityCost` arm `:2186-2211`): the existing `:2186` arm handles `"[enchanted/equipped] <type>'s activated abilities cost {N} less to activate"` — it does NOT handle Hulk's `"power-up abilities of <subject> cost {N} less to activate"` (different subject grammar; `<subject>` = "other creatures you control"). Add a new combinator in the **clean `:2186` style**: `nom_on_lower(...)` wrapping `tag("power-up abilities of ")`, capture `<subject>` via `take_until(" cost ")`, then `tag(" cost ")`, `delimited(tag("{"), nom_primitives::parse_number, tag("}"))`, `tag(" less to activate")`. Route `<subject>` through `parse_type_phrase` (the existing `affected` `TargetFilter` extractor, which handles the "other" self-exclusion). Set `keyword: "power-up"`, `amount: N`, `minimum_mana` via `parse_activated_cost_reduction_minimum_mana`, `dynamic_count: None`. Do NOT use `split_once_on`/`scan_contains` (the `:2213` style).
- **Wonder Man static** (`oracle_static/dispatch.rs`, NEW `alt` arm near the boast `ModifyActivationLimit` arm `:2104-2130`): combinator for `"each power-up ability of <subject> can be activated an additional time"` — `tag("each power-up ability of ")`, capture `<subject>` via `take_until(" can be activated an additional time")`, then `tag(" can be activated an additional time")`. Route `<subject>` ("permanents you control") through `parse_type_phrase` → `affected`. Build `StaticMode::ModifyActivationLimit { keyword: "power-up", new_limit: 2 }` (base 1 + 1).
- **Quinjet phrase** (`oracle_effect/mana.rs`, NEW `alt` arm in `parse_mana_spend_restriction` alongside `tag("to activate abilities")` `:1374-1380`): after `tag("spend this mana only ")`, add `value((), tag("to activate power-up abilities"))` → `ManaSpendRestriction::ActivateTagged(AbilityTag::PowerUp)`. Try this arm BEFORE the broader `"to activate abilities"` arm (more-specific-first), so Quinjet does not collapse to `ActivateOnly`.
- **Kang clause** (parsed inside the power-up effect chain): `preceded(tag("during that turn, "), tag("power-up abilities can't be activated"))` → `Effect::AddRestriction` building `GameRestriction::ProhibitActivity` with `activity: ProhibitedActivity::ActivateAbilities { exemption: ActivationExemption::None, only_tag: Some(AbilityTag::PowerUp) }` and `Duration::UntilEndOfNextTurnOf { player: Controller }` (which lowers to the new pre-armed expiry — B1).
- **Marvel Boy trigger** (`oracle_trigger.rs`, NEW `tag` arm near `"a boast ability"` `:3910-3917`): `tag("a power-up ability")` → `TriggerMode::KeywordAbilityActivated(AbilityTag::PowerUp)`. The dual-condition "whenever … and whenever you activate a power-up ability" splits via `split_and_when_compound` (`:4656`) into two `TriggerDefinition`s.
The **parser is the detector** throughout.

### 2.6 Extension vs Creation
- **Extension/parameterization**: `AbilityTag::PowerUp` (extends the 6-member family); `ProhibitedActivity::ActivateAbilities.only_tag` (parameterizes the existing variant); the `effective_activation_limit` refactor (extracts shared scan); the static/trigger parser `alt` arms.
- **Genuinely new infrastructure (owned, not hand-waved)**: `ManaRestriction::OnlyForTaggedActivation` (new axis — keyed on ability tag, not permanent type); `RestrictionExpiry::UntilEndOfNextTurnOf` (new temporal anchor — mirrors the existing `Duration::UntilEndOfNextTurnOf` pattern, reuses its convert-at-untap arming + the existing `EndOfTurn` cleanup prune); the `ability_tag` field on `PaymentContext::Activation` + threading through the cost-payment chain (M4). Designed in full below.

### 2.7 Analogous Trace (hard gate)
**Traced Boast/Exhaust end-to-end** (the closest labeled, once-restricted, cost-reduced activated ability class):
`oracle.rs:2428` (Exhaust em-dash line → `ability_tag = Some(AbilityTag::Exhaust)` + `OnlyOnce`; power-up clones this) → `types/ability.rs:11627` (`AbilityTag`) → activation flow `handle_activate_ability` (`casting.rs:11661`) which calls `is_blocked_by_cant_activate_abilities` (`casting.rs:460`), `apply_cost_reduction` (`casting.rs:12507`), and the cost-payment chain `pay_ability_cost_for_activation` (`costs.rs:285`) → `pay_ability_cost_inner` (`costs.rs:333`) → `pay_ability_mana_cost*` (`casting.rs:10407+`) building `PaymentContext::Activation` (`casting.rs:10448`) → restriction gate via `check_activation_restrictions` reading `effective_activation_limit` (`restrictions.rs:562`) → `emit_keyword_ability_event_if_tagged` (`casting_targets.rs:657`) → `GameEvent::KeywordAbilityActivated` → `TriggerMode::KeywordAbilityActivated` matcher (`trigger_matchers.rs:3918`).

**Also traced the end-of-next-turn arming idiom** (the closest analog for Kang's B1 expiry): `Duration::UntilEndOfNextTurnOf` (`ability.rs:1512-1521`, doc: "armed (converted to `UntilEndOfTurn`) at the player's next untap step so the existing cleanup-step prune ends it") → `prune_until_next_turn_effects` (`layers.rs:409-426`, converts `UntilEndOfNextTurnOf{Controller}` → `UntilEndOfTurn` for transient effects) and the parallel casting-permission arming (`layers.rs:252-294`) → both called at the untap step (`turns.rs:724`) → the existing `EndOfTurn` cleanup prune (`turns.rs:1356` for transient effects, `:1362-1370` for `ProhibitActivity` restrictions). **Power-up's Kang expiry mirrors this exact pattern at the `RestrictionExpiry` layer.**

**Power-up follows these exact paths.** Divergences: `OnlyOnce` (per-game) instead of `OnlyOnceEachTurn`; the entered-this-turn `SelfManaValue` reduction; the tag-scoped mana restriction (requires threading the tag into `PaymentContext::Activation`); the Kang future-turn prohibition (requires the new mirror-expiry + `only_tag`).

### 2.8 Variant Discoverability — `/add-engine-variant` gate results
`grep data/engine-inventory.json` confirmed: **no `PowerUp` / `power-up` references** (clean slate). Four variant proposals routed through the gate:

**(a) `AbilityTag::PowerUp` — APPROVED (new sibling leaf, correct).**
- *Parameterize-vs-sibling*: `AbilityTag` is a flat enumeration of keyword origins (Boast/Evolve/Exhaust/Outlast/Cycling/Backup). Each tag is an opaque identity class label with no comparator/scope/aggregator axis to lift. Power-up is a new keyword origin → a new leaf is the only correct shape. **Not** a sibling-cluster smell.
- *Categorical boundary*: all `AbilityTag` variants live in keyword-ability rule space (CR 702.x) + activation-event machinery (CR 602.1). Single section. ✔
- *Lockstep*: forces a new arm in the wildcard-free `match` inside `effective_activation_limit` (`restrictions.rs:577-589`) → promoted to `AbilityTag::keyword_str()`. The emit/matcher paths are tag-agnostic (no change). ✔

**(b) `ManaRestriction::OnlyForTaggedActivation(AbilityTag)` — APPROVED (new sibling along a genuinely new axis).**
- *Parameterize-vs-sibling*: `OnlyForTypeSpellsOrAbilities { spell_type, ability: AbilityActivationScope }` keys on the **source permanent's type** (`allows_activation` is `fn(&self, source_types, source_subtypes)` — `mana.rs:471`); `OnlyForActivation` permits **any** activation. Neither expresses "only power-up-tagged abilities" — the discriminating axis is the **ability's keyword tag**, not the permanent's type. New structural axis → new variant. (A parameterization of `OnlyForTypeSpellsOrAbilities` would conflate permanent-type with ability-tag, two different concerns.) Generalizes to any future tag. Built for the class.
- *Categorical boundary*: CR 106.6 (mana spend restriction) — same section as every other `ManaRestriction`. ✔
- *Lockstep*: requires (1) extending `PaymentContext::Activation` with `ability_tag: Option<AbilityTag>`, (2) changing `allows_activation`'s signature to receive the tag and adding the arm, (3) threading the activating tag through the cost-payment **signature family** to all production `PaymentContext::Activation` construction sites (M4 — see reframed scope below), (4) lowering `ManaSpendRestriction::ActivateTagged → ManaRestriction::OnlyForTaggedActivation` in `effects/mana.rs`. Exhaustive matches + the new ctx field enforce completeness. ✔
- *Reuse consideration*: evaluated reusing `OnlyForAny` (disjunctive combinator) — rejected; the inner branch still needs a leaf that matches an ability tag, which does not exist. The leaf is the new variant; `OnlyForAny` remains the composition layer above it.

**(c) `ProhibitedActivity::ActivateAbilities { exemption, only_tag: Option<AbilityTag> }` — APPROVED (parameterization, preserves existing axis).**
- *Parameterize-vs-sibling*: the existing variant `ActivateAbilities { exemption: ActivationExemption }` prohibits **all** ability activations for affected players (optionally exempting mana abilities). Kang prohibits **only power-up** abilities. This is a *restricting* parameterization of the same variant along a new optional axis — NOT a new sibling. Adding `only_tag: Option<AbilityTag>` (`None` = all activations, backward-compatible; `Some(PowerUp)` = power-up only) is the parameterize-don't-proliferate move. A new `ProhibitedActivity::ActivatePowerUp` sibling would be a one-card special case + sibling-cluster smell — **rejected**.
- *Categorical boundary*: CR 101.2 + CR 602.5 (activation prohibition) — same section as the existing variant. ✔
- *Lockstep (FIX 2 — SEVEN sites, not six)*: `exemption` field is **preserved**. New field gets `#[serde(default, skip_serializing_if = "Option::is_none")]`. The single runtime consult arm (`is_blocked_by_cant_activate_abilities`, `casting.rs:469`) is updated to also compare `activating_ability.ability_tag` against `only_tag`; the `activating_ability: &AbilityDefinition` is ALREADY a param there (`casting.rs:463`) → **no new param threading at the consult site**. There are **seven** total `ActivateAbilities {` construction/match sites (`grep "ActivateAbilities {"`):
  - **Need updating** (literal construction or `matches!` with no `..` inside the braces — will FAIL to compile until `only_tag` is added):
    1. `types/ability.rs:1580` — the enum definition itself.
    2. `casting.rs:469` — the runtime consult destructure (`{ exemption }` → `{ exemption, only_tag }`).
    3. `casting.rs:15312` — test literal construction.
    4. `derived_views.rs:1255` — literal construction.
    5. `ability_utils.rs:5955` — literal construction.
    6. `effects/add_restriction.rs:208` and `:242` — two test literal constructions.
    7. `parser/oracle_effect/mod.rs:2174` — parser literal construction.
    8. **`parser/oracle_effect/mod.rs:42657` — the 7th `ActivateAbilities {` SITE the prior plan missed: a `matches!(... activity: ProhibitedActivity::ActivateAbilities { exemption: ActivationExemption::ManaAbilities })` test assertion with NO `..` inside the inner braces — it will fail to compile unless its braces gain `, ..` or `only_tag: _`.** Add `, ..` to the inner `ActivateAbilities { … }` pattern.
  - **Unaffected** (already use `..` inside the braces): `derived_views.rs:392` (`ActivateAbilities { .. }`), `add_restriction.rs:226` and `:260` (both `ActivateAbilities { .. }`).
  - **Note:** the compiler enforces lockstep (every literal/no-`..` match breaks until updated), but the enumeration above must be complete so no site is missed during implementation. Edit each surgically per multi-agent-safety.
- *Coarse projection*: `derived_views.rs:392` (`ActivateAbilities { .. }` → `PlayerConditionKind::CantActivateAbilities`) uses `..` — no change; the projected status is now coarse for a power-up-only prohibition (says "can't activate abilities"). Correctness is unaffected (the consult site is precise). Flagged §4 item 4. ✔

**(d) `RestrictionExpiry::UntilEndOfNextTurnOf { player: PlayerId }` — APPROVED (new temporal-anchor leaf mirroring `Duration::UntilEndOfNextTurnOf`; parameterization N/A).**
- *Parameterize-vs-sibling*: `RestrictionExpiry` is a 3-variant enum of **distinct temporal anchors** (`EndOfTurn`, `EndOfCombat`, `UntilPlayerNextTurn { player }`; `ability.rs:1586-1590`). Each variant is a different *point in the turn structure* at which a restriction expires — there is no shared comparator/scope/aggregator axis; the discriminator IS the temporal anchor itself. `UntilEndOfNextTurnOf` is a fourth distinct anchor: "the END (cleanup) of `player`'s next turn, persisting through that whole turn." **A new leaf IS still needed** — the restriction-expiry enum has no end-of-next-turn member, and the convert-at-untap arming needs a pre-arm marker distinct from `EndOfTurn` (which would expire at the *creation* turn's cleanup). The justification is by **ANALOGY to the existing `Duration::UntilEndOfNextTurnOf` pattern** (`ability.rs:1512-1521`): the `Duration` enum already carries exactly this anchor and is armed by converting to `UntilEndOfTurn` at the grantee's untap step (`layers.rs:409-426`), with the existing cleanup prune ending it. We mirror that anchor + arming onto `RestrictionExpiry`: at `player`'s untap step, convert `UntilEndOfNextTurnOf { player }` → the EXISTING `RestrictionExpiry::EndOfTurn`; the existing cleanup prune (`turns.rs:1362-1370`, which ALREADY drops `EndOfTurn` for `ProhibitActivity`) then ends it at that turn's cleanup. **Zero new cleanup-prune code; no `bool` field** (the convert-at-untap provides temporal arming for free, exactly as it does for `Duration`).
- *Categorical boundary*: CR 514.2 (cleanup-step "this turn"/"until end of turn" effects end) + CR 500.7 (extra turns). The new variant prunes (after conversion) at CR 514.2 of the player's next turn. Single concern (turn-structure expiry). ✔
- *Lockstep*: the new variant must be handled at the untap step (`turns.rs:737-746`): a mutating pre-pass (mirroring `prune_until_next_turn_effects` at `:724`) converts `UntilEndOfNextTurnOf { player } where player == active` → `EndOfTurn` **before** the existing `state.restrictions.retain(...)` block (which drops only `UntilPlayerNextTurn` and must NOT catch the new variant). After conversion, the existing cleanup prune (`turns.rs:1362-1370`) ends it. The end-of-combat prune is unaffected (`UntilEndOfNextTurnOf` is not `EndOfCombat`). Exhaustive `RestrictionExpiry` matches at every site force the compiler to flag the new variant. ✔

**(e) NO new variant for the dynamic self-MV cost reduction, Hulk's static, or the Marvel Boy trigger** — all reuse existing variants (`CostReduction` + `SelfManaValue` + `SourceEnteredThisTurn`; `ReduceAbilityCost`; `ModifyActivationLimit`; `KeywordAbilityActivated`). Parameterize-don't-proliferate win.

### 2.9 Verification Matrix

| Behavioral claim | Changed seam | Production entry point | Discriminating runtime test | Revert-failing assertion | Sibling/negative case | Coverage impact |
|---|---|---|---|---|---|---|
| Power-up ability parses as tagged activated ability | `oracle.rs` power-up block | rehydrate real card | Nick Fury / Jack of Hearts cast via `GameRunner` activation | counters land; `ability_tag == PowerUp` | a non-power-up activated ability stays `ability_tag: None` | new cards become supported (post regen) |
| "Activate only once" enforced **per game** | `OnlyOnce` arm (`restrictions.rs:725`) made limit-aware | activate across turns | activate Nick Fury T1, attempt again T3 → illegal; `activated_abilities_this_game == 1` | second activation rejected | a per-turn `MaxTimesEachTurn` ability still resets per turn | — |
| Cost reduced by **source printed MV** when entered this turn (generic-only) | `apply_cost_reduction` + `CostReduction` (M3) | cast Hulk, activate same turn | Hulk power-up `{6}{R}{G}` reduces generic by MV(5)=5 the entering turn (→ `{1}{R}{G}`); **full** cost next turn | reduction absent → afford check fails this turn / passes next | a power-up source that entered a prior turn gets NO reduction | — |
| Hulk static reduces **other** power-up abilities by {3} | static-reduction keyword gate generalized (`casting.rs:12554`) honoring `affected`+`minimum_mana` | Hulk + another power-up creature | other creature's power-up generic −3; **Hulk's own** unaffected ("other") | remove Hulk → reduction gone | a non-power-up activated ability unaffected | — |
| Wonder Man raises power-up limit to 2 (**per game**) | `OnlyOnce` arm via `effective_activation_limit_per_game` (M1) | Wonder Man + power-up creature | that creature activates its power-up **twice** total; third illegal | Wonder Man gone → second illegal | Wonder Man does not raise a non-power-up `OnlyOnce` ability | — |
| Quinjet {R}{R} spends only on power-up | `ManaRestriction::OnlyForTaggedActivation` + `PaymentContext::Activation.ability_tag` threaded (M4/M5) | tap Quinjet {R}{R}, attempt various payments | the {R}{R} pays a power-up activation; rejected for a normal ability and for casting; **the {R} ability is unrestricted** (M5) | restriction absent → mana pays anything | unrestricted {R} from the other Quinjet ability pays anything | — |
| Kang prohibits power-up **throughout** the extra turn | `ProhibitActivity { only_tag: Some(PowerUp) }` (B2) + `RestrictionExpiry::UntilEndOfNextTurnOf` (B1) | resolve Kang, enter extra turn | during the **entire** extra turn every power-up ability is illegal (checked at untap/main/end); non-power-up legal; expires at the turn AFTER the extra turn | prohibition absent → power-up activatable in extra turn | the prohibition does NOT expire at the extra turn's start (untap converts to `EndOfTurn`, not drop); it survives to that turn's cleanup | — |
| Marvel Boy triggers on power-up activation | `KeywordAbilityActivated(PowerUp)` (tag-agnostic matcher) | activate any power-up ability | Marvel Boy gains +1/+1 | trigger absent → no counter | activating a non-power-up ability does NOT trigger it | — |
| Advancing the Spirit per-turn alt-cost — **DEFERRED** | none | none | none (out of scope) | n/a | n/a | **remains unsupported / coverage-red** (§2.10) |

**Parser honesty:** No Oracle text is accepted-but-deferred for the seven named cards' covered clauses. Nick Fury's sub-effects (look-at-top, conditional PTB, DFC transform, random-bottom) must each be verified to be consumed by a handler during implementation (per the `parsed_ast_not_consumed` lesson); any sub-effect not yet consumed stays `Effect::unimplemented(...)` so its coverage remains red. Advancing the Spirit's alt-cost clause MUST parse to `Effect::unimplemented(name, fragment)` (never a silent no-op) so coverage honestly reports it unsupported.

### 2.10 Honest Deferral — Advancing the Spirit
`Advancing the Spirit`: `You may pay {0} rather than pay the power-up cost of the first power-up ability you activate during each of your turns.` This is a **per-turn alternative-cost replacement keyed on power-up abilities** — it requires (a) a once-per-your-turn counter on a replacement-style cost substitution, (b) recognizing "the power-up cost" of an arbitrary power-up ability, and (c) an optional alt-cost payment window at activation. None of this infrastructure exists, and it is a different mechanic from the labeled-activated-ability class. **Out of scope for this wave.** The parser must emit `Effect::unimplemented(...)` for the alt-cost clause (the ETB "draw a card" clause parses normally); coverage stays red for the card. Do NOT attempt a partial/silent implementation.

---

## 3. Step-by-Step Implementation Plan

> Verification cadence per CLAUDE.md: `cargo fmt --all` directly; everything else via Tilt (`tilt logs clippy/test-engine`, `./scripts/tilt-wait.sh`). Do NOT run `cargo build/clippy/test` directly.

### Step 1 — Add `AbilityTag::PowerUp` + `keyword_str()` (gate (a))
- **`types/ability.rs:11645`** — add to `AbilityTag` (after `Backup`): `/// CR 602.5b + CR 602.1: This ability originated from a Power-up keyword definition.\nPowerUp,`. (Re-grep `602.5b` and `602.1` before writing.)
- **`restrictions.rs:577-589`** — extract the tag→keyword `match` into a method `AbilityTag::keyword_str(self) -> &'static str` on the enum (`types/ability.rs`), add `PowerUp => "power-up"`, replace the inlined match with `tag.keyword_str()`. The wildcard-free match forces the new arm (compiler-enforced).
- No change to `emit_keyword_ability_event_if_tagged` (`casting_targets.rs:657`) — the generic path emits `KeywordAbilityActivated { ability_tag: PowerUp, .. }` (Cycling is the only special-case).

### Step 2 — Parser: `Power-up —` activated-ability line (Exhaust template — FIX 5)
- **`oracle.rs`**, modeled on the **Exhaust** block (`:2428-2455`), add a parallel block:
  - `nom_on_lower(&line, &lower, |i| value((), alt((tag("power-up \u{2014} "), tag("power-up -- ")))).parse(i))`.
  - `find_activated_colon` → split; `parse_oracle_cost`; `parse_effect_chain_with_context(&effect_text, AbilityKind::Activated, &mut ctx)`.
  - `def.activation_restrictions.push(ActivationRestriction::OnlyOnce)` — **CR 602.5b** (per-game; reminder text says "Activate each power-up ability only once" with no "each turn"). (Exhaust already does exactly this push — power-up clones it directly. Boast would be wrong: it pushes `OnlyOnceEachTurn`.)
  - `def.ability_tag = Some(AbilityTag::PowerUp)` (mirrors Exhaust's `def.ability_tag = Some(AbilityTag::Exhaust)`).
  - `def.cost_reduction = Some(CostReduction { amount_per: 1, count: QuantityExpr::Ref { qty: QuantityRef::SelfManaValue }, condition: Some(ParsedCondition::SourceEnteredThisTurn) })`. **CR 602.2b + CR 601.2f + CR 302.6.** (Set from the keyword definition, not parsed from the reminder, which `strip_reminder_text` removes.)
- CR annotations: `// CR 602.5b: power-up may be activated only once per game.` and `// CR 602.2b + CR 601.2f + CR 302.6: cost reduced by source mana value if it entered this turn.`

### Step 3 — Dynamic self-MV cost reduction already works (M3 — verify, do not rebuild)
- **Evidence (read live):** `apply_cost_reduction` (`casting.rs:12513-12530`) (i) gates on `reduction.condition` via `evaluate_condition` (`:12517-12519`), (ii) resolves `reduction.count` via `super::quantity::resolve_quantity` (`:12522`), (iii) reduces **generic only** via `reduce_generic_in_cost` (`:12526`). `QuantityRef::SelfManaValue` resolves to the **source permanent's printed mana value** (`obj.mana_cost.mana_value_with_x(...)` = the card's CMC, NOT the activation cost's MV) — verified at `quantity.rs:1663-1675` (doc: "reads the source object's printed mana value"). This grounds "reduce the cost by his mana cost" = reduce generic by the source creature's printed MV, gated on it having entered this turn. **No code change** — only the test in Step 9 asserting the exact reduced cost.

### Step 4 — Hulk: class-scoped static cost reduction (parser + runtime gate)
- **`oracle_static/dispatch.rs`** — add the NEW arm described in §2.5 near the existing `ReduceAbilityCost` arm (`:2186-2211`), in the **clean `:2186` combinator style** (`nom_on_lower` + `tag`/`take_until`/`delimited`), NOT the `:2213` `split_once_on`/`scan_contains` style. Build `StaticMode::ReduceAbilityCost { keyword: "power-up", amount: N, minimum_mana: parse_activated_cost_reduction_minimum_mana(tp.lower), dynamic_count: None }` with `.affected(<subject filter from parse_type_phrase>)`.
- **`casting.rs:12554` (`apply_static_activated_ability_cost_reduction`)** — the gate is currently `if keyword != "activated" || *amount == 0 { continue; }`. Generalize to ALSO match the **activating ability's tag keyword**: compute `let active_kw = ability_def.ability_tag.map(AbilityTag::keyword_str);` (the `ability_def: &mut AbilityDefinition` is already a param) and change the gate to `if (keyword != "activated" && Some(keyword.as_str()) != active_kw) || *amount == 0 { continue; }`. This site already honors `affected` (`:12557-12566`, evaluated against the activating `source_id`, so Hulk's "other" excludes itself) and `minimum_mana` (`:12567`). Do NOT also route through the `keywords.rs` helper (it ignores both `affected` and `minimum_mana`) — single site, no double-reduction.
- CR: `// CR 601.2f: power-up ability cost reduced by a controlled source's static, scoped via affected + minimum_mana.`

### Step 5 — Wonder Man: raise the per-game activation limit (M1)
- **`oracle_static/dispatch.rs`** — add the NEW `ModifyActivationLimit` arm described in §2.5 near the boast arm (`:2104-2130`): `keyword: "power-up"`, `new_limit: 2`, `affected` from `<subject>` ("permanents you control").
- **M1 — per-game limit path (refactor; no scope conflation):**
  1. Extract the `ModifyActivationLimit` static-scan from `effective_activation_limit` (`restrictions.rs:590-621`) into `activation_limit_from_statics(state, player, source_id, keyword: &str) -> u32` (base 1, scans statics, returns `limit.max(new_limit)`). This static is **scope-agnostic** — it raises a cap regardless of per-turn/per-game; verified it reads NO counter.
  2. `effective_activation_limit` (per-turn) becomes: resolve `keyword = tag.keyword_str()`, return `activation_limit_from_statics(state, player, source_id, keyword)`. Its caller (the `OnlyOnceEachTurn` arm, `restrictions.rs:714-722`) still reads `activated_abilities_this_turn`.
  3. Add `effective_activation_limit_per_game(state, player, source_id, ability_index) -> u32` — resolves the tag keyword the same way and calls `activation_limit_from_statics`. (Returns only the cap; the counter comparison stays in the consult arm.)
  4. **`restrictions.rs:725-732` (`OnlyOnce` arm)** — change the hard `== 0` to limit-aware:
     ```rust
     ActivationRestriction::OnlyOnce => {
         let count = state.activated_abilities_this_game.get(&key).copied().unwrap_or(0);
         count < effective_activation_limit_per_game(state, player, source_id, ability_index)
     }
     ```
     This reads `activated_abilities_this_game` (NOT `_this_turn`), so per-game and per-turn counters/scopes are never conflated. The ONLY shared code is the static-scan (`activation_limit_from_statics`), which makes no per-turn assumption.
- **Design note:** power-up's base limit is once-per-**game** (`OnlyOnce`). "Can be activated an additional time" with no per-turn qualifier is read as **per game** (cap 2 total). Flagged for a maintainer nod (§4 item 1).
- CR: `// CR 602.5b: ModifyActivationLimit raises the once-per-game power-up cap.`

### Step 6 — Quinjet: tag-scoped mana spend restriction (gate (b); M4/M5)
- **`types/ability.rs:1491` (`ManaSpendRestriction`)** — add parser-level `ActivateTagged(AbilityTag)`. Update `resolve_restrictions` (`effects/mana.rs:257-316`) with the arm `ManaSpendRestriction::ActivateTagged(t) => Some(ManaRestriction::OnlyForTaggedActivation(*t))`.
- **`types/mana.rs:360` (`ManaRestriction`)** — add `OnlyForTaggedActivation(AbilityTag)`.
- **`types/mana.rs:268-281` (`PaymentContext::Activation`)** — add field `ability_tag: Option<AbilityTag>`.
- **`types/mana.rs:471` (`allows_activation`)** — change signature to `fn allows_activation(&self, source_types: &[String], source_subtypes: &[String], ability_tag: Option<AbilityTag>) -> bool` and add arm `ManaRestriction::OnlyForTaggedActivation(t) => ability_tag == Some(*t)`. Update the `OnlyForAny` recursive call to forward `ability_tag`. The spell-only block returns `false` for `OnlyForTaggedActivation` (it permits no spell).
- **`types/mana.rs:513-523` (`allows`)** — update the `PaymentContext::Activation { source_types, source_subtypes, ability_tag }` destructure and forward `ability_tag` to `allows_activation`.
- **M4 (REFRAMED — thread `ability_tag` through the `pay_ability_*` signature family, ~8 call sites; FIX 4).** The real work is NOT "6 `PaymentContext::Activation` literal sites" — it is threading a new `ability_tag: Option<AbilityTag>` parameter through the cost-payment **signature family**, which currently carries `source_id` but no `ability_index`/`ability_tag`. The `ResolvedAbility`/`AbilityDefinition` that carries the tag is in scope at every chain top, so each top reads `ability.ability_tag` / `ability_def.ability_tag` and threads it down. The signatures to extend:
  - `pay_ability_cost_for_activation` (`costs.rs:285`) → `pay_ability_cost_inner` (`costs.rs:333`, `PaymentScope::Activation`) → the `pay_ability_mana_cost*` family: `pay_ability_mana_cost` (`casting.rs:10407`), `pay_ability_mana_cost_excluding` (`casting.rs:10417`), `pay_ability_mana_cost_with_choices_excluding` (`casting.rs:10436`, the PRIMARY spend site building `PaymentContext::Activation` at `:10448`) → and `pay_mana_sub_cost` (`mana_abilities.rs:2214`, which calls `pay_ability_mana_cost_excluding` at `:2224`).
  - **~8 call sites** must pass the threaded tag (locate by symbol — cited line numbers may be stale): `pay_ability_mana_cost_excluding` alone has callers at `casting_targets.rs:482`, `mana_abilities.rs:2224`, `casting_costs.rs:7314` (the prior plan cited `7326/7490`; locate by symbol), plus the `costs.rs:412` chain (`pay_ability_cost_inner` body) and the chain tops at `casting_targets.rs:536` (`pay_ability_cost_for_activation`) and `casting_costs.rs:2316`. The sites that already carry an index/pending (`casting_costs.rs:6541` `auto_tap_mana_sources_inner` via `ManaSourceOption.ability_index`; `casting_costs.rs:7281`-region `apply_committed_assist` via `PendingCast.activation_ability_index`; `engine.rs:3084` Phyrexian validation via `pending_ref.activation_ability_index`) read the tag from `state.objects[...].abilities[idx].ability_tag` rather than receiving it as a param.
  - **Single-authority note:** add `ability_tag: Option<AbilityTag>` as a parameter to `pay_ability_cost_for_activation` → `pay_ability_cost_inner` (Activation scope) → the `pay_ability_mana_cost*` family + `pay_mana_sub_cost`, and read-from-context at the three sites that already have an index/pending. **Missing any site = Quinjet mana silently pays the wrong things** — but the new non-`None`-defaulted `ability_tag` field on `PaymentContext::Activation` makes every struct literal a compile error until updated, and the signature changes make every caller a compile error until threaded, so the compiler surfaces all of them and enforces lockstep.
  - Test-only `PaymentContext::Activation` sites (`mana.rs:516/1616/1957/1961`, `mana_abilities.rs:3416/3429`, `mana_payment.rs:2965`) pass `ability_tag: None` unless the test exercises tag scoping.
- **`oracle_effect/mana.rs:1374` (`parse_mana_spend_restriction`)** — add the `alt` arm `value((), tag("to activate power-up abilities"))` → `ManaSpendRestriction::ActivateTagged(AbilityTag::PowerUp)`, BEFORE the `"to activate abilities"` arm (more-specific-first).
- **M5 — Quinjet's two mana abilities:** Quinjet has `{T}: Add {R}.` (unrestricted) AND `{T}: Add {R}{R}. Spend this mana only to activate power-up abilities.` (restricted). The restriction attaches to the **{R}{R} ability only** (parsed from its trailing sentence); the {R} ability has no `ManaSpendRestriction`. Both share `{T}` (mutually exclusive — both tap). The parser already produces two separate mana abilities. Tests (Step 9) assert: the {R}{R} mana pays a power-up activation but is rejected for a normal ability/spell; the {R} mana pays anything.
- CR: `// CR 106.6: mana spendable only to activate power-up-tagged abilities.`

### Step 7 — Kang: prohibition scoped to power-up, active throughout the extra turn (B1 + B2)
The power-up effect chain produces the +1/+1 counter and the extra turn (existing effects). The prohibition clause is appended.

**B2 — `ProhibitedActivity::ActivateAbilities.only_tag` (FIX 2 — seven sites):**
- **`types/ability.rs:1580`** — change the variant to `ActivateAbilities { exemption: ActivationExemption, #[serde(default, skip_serializing_if = "Option::is_none")] only_tag: Option<AbilityTag> }`. (`exemption` PRESERVED; new field defaults to `None` = current "all activations" behavior.)
- **`casting.rs:469` (`is_blocked_by_cant_activate_abilities`)** — the destructure becomes `activity: ProhibitedActivity::ActivateAbilities { exemption, only_tag }`. After the `caster_affected` check and the `exemption` check, add: if `only_tag` is `Some(tag)`, the prohibition applies only when `activating_ability.ability_tag == Some(tag)` (the `activating_ability: &AbilityDefinition` is already a param — `casting.rs:463` — no new threading at the consult site). So a power-up-only prohibition does not block non-power-up activations.
- **Construct/match sites (B2 lockstep — SEVEN `ActivateAbilities {` sites total):**
  - **Add `only_tag: None` to literal constructions** (compile errors enforce this): `casting.rs:15312` (test), `derived_views.rs:1255`, `ability_utils.rs:5955`, `add_restriction.rs:208` (test), `add_restriction.rs:242` (test), `parser/oracle_effect/mod.rs:2174` (parser).
  - **Update the `matches!` test assertion at `parser/oracle_effect/mod.rs:42657`** — its inner `ProhibitedActivity::ActivateAbilities { exemption: ActivationExemption::ManaAbilities }` has NO `..` inside the braces and WILL FAIL to compile; add `, ..` (so it becomes `ActivateAbilities { exemption: ActivationExemption::ManaAbilities, .. }`). **This is the 7th site the prior plan missed.**
  - **No change** to the wildcard-`..` sites: `derived_views.rs:392`, `add_restriction.rs:226`, `add_restriction.rs:260` (all `ActivateAbilities { .. }`).
  - (`#[serde(default)]` covers deserialization of older serialized state; struct literals + the no-`..` match still need the field/`..`. Edit each surgically per multi-agent-safety.)
- **`derived_views.rs:392`** projects a power-up-only prohibition as the generic "can't activate abilities" status (coarse). Correctness is unaffected (the consult site is precise). Flagged §4 item 4.
- CR: `// CR 101.2 + CR 602.5: prohibition optionally scoped to a single ability tag (None = all activations).`

**B1 — `RestrictionExpiry::UntilEndOfNextTurnOf { player }` (NEW temporal anchor; mirror the existing convert-to-`EndOfTurn` arming idiom — FIX 1):**
- **Why a new leaf is still needed (by analogy, not by false "no pattern" claim):** the `Duration` enum ALREADY has `UntilEndOfNextTurnOf` (`ability.rs:1512-1521`) for exactly "persists through the player's whole next turn, expires at its cleanup", and it is armed by **converting to `UntilEndOfTurn` at the grantee's untap step** (`prune_until_next_turn_effects`, `layers.rs:409-426`; parallel casting-permission arming `layers.rs:252-294`), after which the existing cleanup prune ends it. The `RestrictionExpiry` enum has NO end-of-next-turn member (only `EndOfTurn`/`EndOfCombat`/`UntilPlayerNextTurn`), and the convert-at-untap arming needs a pre-arm marker distinct from `EndOfTurn` (a bare `EndOfTurn` would expire at the *creation* turn's cleanup). So we add a leaf that **mirrors `Duration::UntilEndOfNextTurnOf`** and reuse its arming pattern at the `RestrictionExpiry` layer. **No `bool` latch; no new cleanup-prune code.**
- **`types/ability.rs:1586-1590`** — add to `RestrictionExpiry`: `/// CR 514.2 + CR 500.7: Mirrors `Duration::UntilEndOfNextTurnOf`. Created pre-armed; at `player`'s next untap step it is CONVERTED to `RestrictionExpiry::EndOfTurn` (mirroring `prune_until_next_turn_effects`), so the existing cleanup-step prune ends it at that turn's cleanup. It survives the creating turn's own cleanup because that turn's untap step already passed before creation.\nUntilEndOfNextTurnOf { player: PlayerId },`.
- **`turns.rs:737` (untap, `execute_untap_with_choices`) — the conversion site (mirror `layers.rs:409-426`, called just above at `:724`).** BEFORE the existing `state.restrictions.retain(...)` block (`:737-746`), add a mutating pre-pass:
  ```rust
  // CR 514.2: arm "until end of the player's next turn" restrictions when the
  // player's next turn begins — convert to EndOfTurn so the cleanup-step prune
  // (turns.rs:1362) ends them at THIS turn's cleanup, persisting through the
  // whole turn. Mirrors prune_until_next_turn_effects (layers.rs:409).
  for r in state.restrictions.iter_mut() {
      if let GameRestriction::ProhibitActivity { expiry, .. } = r {
          if matches!(expiry, RestrictionExpiry::UntilEndOfNextTurnOf { player } if *player == active) {
              *expiry = RestrictionExpiry::EndOfTurn;
          }
      }
  }
  ```
  Then leave the existing `retain` (`:737-746`) UNCHANGED — it matches `ProhibitActivity { expiry, .. }` and drops only `UntilPlayerNextTurn`, so it does NOT catch `UntilEndOfNextTurnOf` (now already converted to `EndOfTurn`, which it also does not drop here). Confirmed (verified read): the untap `retain` drops only `UntilPlayerNextTurn`; `EndOfTurn` is dropped at cleanup, not untap.
- **Cleanup prune — ALREADY covers it (verified; ZERO new code).** `turns.rs:1362-1370` (`execute_cleanup`) already does `state.restrictions.retain(...)` dropping both `DamagePreventionDisabled` and **`ProhibitActivity` with `expiry: RestrictionExpiry::EndOfTurn`** (`:1366-1367` `| GameRestriction::ProhibitActivity { expiry, .. } => !matches!(expiry, RestrictionExpiry::EndOfTurn)`). After the untap conversion, Kang's restriction is `EndOfTurn` and this existing arm drops it at the extra turn's cleanup. **No cleanup-prune change required.**
- **`add_restriction.rs:84-102` (`fill_runtime_fields`)** — split the duration-lowering: map `Duration::UntilEndOfNextTurnOf { player: Controller }` (Kang's clause) → `RestrictionExpiry::UntilEndOfNextTurnOf { player: ability.controller }` (pre-armed; no `bool`); keep `Duration::UntilNextTurnOf { Controller }` → `UntilPlayerNextTurn`. The existing doc comment notes "no printed restriction uses 'end of next turn'" — Kang is now the first, so this split is the correct fix. Verify no other card relies on `UntilEndOfNextTurnOf → UntilPlayerNextTurn` for a *restriction* (§4 item 3).
- **Parser** — parse `"during that turn, power-up abilities can't be activated"` (composed after the extra-turn effect) into `Effect::AddRestriction` building `GameRestriction::ProhibitActivity { source: <filled>, affected_players: <your/controller scope>, expiry: <filled from duration>, activity: ProhibitedActivity::ActivateAbilities { exemption: ActivationExemption::None, only_tag: Some(AbilityTag::PowerUp) } }` with `duration = Some(Duration::UntilEndOfNextTurnOf { player: PlayerScope::Controller })`. Use `preceded(tag("during that turn, "), tag("power-up abilities can't be activated"))`. ("During that turn" = the extra turn just granted — CR 500.7: added directly after the current one, taken next.)
- CR: `// CR 500.7 + CR 514.2 + CR 602.5: during the granted extra turn, power-up abilities can't be activated; the restriction is armed at the extra turn's untap step (convert to EndOfTurn) and expires at that turn's cleanup.` (Re-grep `500.7`, `514.2`, `602.5` — all verified.)

**FIX 3 — Documented skipped/prevented-extra-turn arming-leak edge (narrow):**
- **The edge (verified):** the arming conversion fires only at `execute_untap_with_choices` (the untap step). If Kang's granted extra turn is **SKIPPED or PREVENTED** before its untap step runs — i.e., a `turns_to_skip` decrement (`turns.rs:472-476`, `return start_next_turn(...)`) or a turn-start replacement `Prevented` (a Stranglehold-class skip at `turns.rs:483-487`, `return start_next_turn(...)`) lands *exactly* on the granted extra turn — then `start_next_turn` recurses past that turn without ever reaching `execute_untap`, so the `UntilEndOfNextTurnOf { player }` restriction is never converted to `EndOfTurn`, never armed, and never pruned. It would then wrongly persist and block the controller's OTHER power-up abilities (on subsequent turns) indefinitely.
- **Why it is narrow:** it requires a skip/prevention to land on precisely the one granted extra turn (not the controller's natural next turn). In practice extra turns are taken (that is the point of granting them); the overlap is a rare corner.
- **Choice (stated):** **ACCEPT as a documented narrow edge** for this wave. Rationale: a fully robust arming point would require either (a) re-checking restriction arming inside the `turns_to_skip`/`Prevented` recursion paths (which `prune_until_next_turn_effects` and the casting-permission arming *also* skip — so fixing it here without fixing the existing `Duration::UntilEndOfNextTurnOf` analog would be inconsistent and out of scope), or (b) a turn-number stamp (rejected in §4 item 2 as LIFO-fragile). The existing `Duration` arming idiom has the identical edge, so accepting it keeps the new `RestrictionExpiry` mirror behaviorally consistent with the pattern it copies. Add a `// NOTE:` comment at the untap conversion site documenting the edge, and add an `#[ignore]`-or-documented test (`power_up_kang_skipped_extra_turn_edge.rs`) that records the known leak (skip the extra turn via `turns_to_skip`, assert the restriction is NOT pruned) so the behavior is captured and a future robust-arming fix has a regression anchor. Surface the edge to the maintainer (§4 item 5).

### Step 8 — Marvel Boy: trigger on power-up activation
- **`oracle_trigger.rs:3910-3917`** — add a `tag` arm near `"a boast ability"`: `if tag::<_,_,OracleError<'_>>("a power-up ability").parse(rest).is_ok() { def.mode = TriggerMode::KeywordAbilityActivated(AbilityTag::PowerUp); return Some((TriggerMode::KeywordAbilityActivated(AbilityTag::PowerUp), def)); }`.
- Marvel Boy's text is a **dual-condition** trigger. `split_and_when_compound` (`:4656`) splits "whenever … and whenever you activate a power-up ability" into two `TriggerDefinition`s sharing one effect (the ETB half already parses; the power-up half uses the new arm). **CR 603.1b** (verified) allows multiple trigger conditions. Verify the split produces two defs during implementation.
- **`trigger_matchers.rs:3918-3944` (`match_keyword_ability_activated`)** — no change; it compares `ability_tag == tag` generically (tag-agnostic), so `PowerUp` flows through.
- CR: `// CR 602.1 + CR 603.1b: triggers when the controller activates a power-up ability (one of two trigger conditions).`

### Step 9 — Discriminating runtime tests (`/card-test`, real-card rehydration)
Add integration tests under `crates/engine/tests/integration/`. Per `parser_fix_inert_until_data_regen` + `test_engine_nextest_card_fixture`, rehydrate real cards via `add_real_card` + rehydrate (NOT `from_oracle_text`); drive activation through `GameScenario`/`GameRunner` per `card-test`. Each test carries a revert-failing assertion.
1. `power_up_once_per_game.rs` — Nick Fury: activate T1 (counters land), advance to T3, assert second activation **illegal** (`legal_actions` excludes it), `activated_abilities_this_game == 1`. Sibling: a `MaxTimesEachTurn` ability resets per turn.
2. `power_up_cost_reduction_entered_this_turn.rs` (M3) — Hulk (MV 5): assert the power-up cost is `{1}{R}{G}` (`{6}{R}{G}` − 5 generic) the entering turn, and full `{6}{R}{G}` the following turn. Negative: a power-up source that entered a prior turn gets no reduction.
3. `power_up_static_cost_reduction.rs` (Hulk static) — Hulk + a second power-up creature: assert the other creature's power-up generic −3; **Hulk's own** unaffected ("other"); remove Hulk → reduction gone. Sibling: a non-power-up activated ability unaffected.
4. `power_up_additional_activation.rs` (Wonder Man, M1) — Wonder Man + power-up creature: assert two total activations legal, third illegal; remove Wonder Man → second illegal. Sibling: Wonder Man does not raise a non-power-up `OnlyOnce` ability.
5. `power_up_mana_restriction.rs` (Quinjet, M4/M5) — produce {R}{R} from the restricted ability: assert it pays a power-up activation, is rejected for a normal activated ability, and is rejected for a spell; assert the **unrestricted {R} ability** pays anything (M5).
6. `power_up_kang_prohibition.rs` (B1/B2) — Kang: resolve, enter the extra turn; assert **every** power-up ability is illegal **throughout** the extra turn (check at untap, main, and end step of the extra turn — not just its start) and non-power-up abilities are legal; assert the prohibition expires at the cleanup of the extra turn (legal again on the following turn). The most error-prone correctness point — the test must assert the restriction survives the extra turn's untap-step conversion (it converts to `EndOfTurn`, it is NOT dropped at untap).
7. `power_up_kang_skipped_extra_turn_edge.rs` (FIX 3 — documented edge) — Kang grants the extra turn but it is skipped (`turns_to_skip` decrement landing on the extra turn); assert (as a recorded KNOWN-LEAK anchor, documented in the test) that the restriction is NOT pruned because the untap conversion never ran. Marks the narrow edge for a future robust-arming fix.
8. `power_up_marvel_boy_trigger.rs` — Marvel Boy: activate any power-up ability → +1/+1 counter; activate a non-power-up ability → **no** counter (negative case).

### Step 10 — Lockstep registration checklist
- **`/add-keyword`**: power-up is parsed as an em-dash activated ability (like Exhaust), NOT synthesized from a `Keyword::` variant → **no `KeywordKind`/`Keyword` enum entry**. Confirm no card-data synthesis path expects `Keyword::PowerUp`.
- **`/add-engine-variant`**: ran the gate for all four proposals (§2.8): `AbilityTag::PowerUp` (a), `ManaRestriction::OnlyForTaggedActivation` (b), `ProhibitedActivity::ActivateAbilities.only_tag` parameterization (c), `RestrictionExpiry::UntilEndOfNextTurnOf` (d) — all APPROVED with reasoning.
- **`/add-engine-effect`** (for `ManaRestriction` + `PaymentContext` field + `RestrictionExpiry` + `ProhibitedActivity`): types → parser → resolver → targeting (n/a) → **multiplayer/serde filter**: every new variant/field derives `Serialize`/`Deserialize`; `PaymentContext` is runtime-only borrowed (`<'a>`) — NOT serialized, confirm it is never persisted; `RestrictionExpiry` is `#[serde(tag="type")]` and `UntilEndOfNextTurnOf { player }` round-trips; `ProhibitedActivity.only_tag` and `ActivateAbilities` round-trip; `AbilityTag::PowerUp` round-trips → frontend (none) → AI (none) → tests.
- **`/add-trigger`**: `TriggerMode::KeywordAbilityActivated` already registered (matcher `trigger_matchers.rs`, serde) — tag-agnostic; no new registry entry.
- **`/add-static-ability`**: `ReduceAbilityCost`/`ModifyActivationLimit` already registered in static-line serde + layer/coverage tables — confirm the new `affected`-honoring `"power-up"` arms round-trip.
- **WASM/MP round-trip**: add a serde round-trip assertion for `RestrictionExpiry::UntilEndOfNextTurnOf { player: PlayerId(1) }` and `ProhibitedActivity::ActivateAbilities { exemption: ActivationExemption::None, only_tag: Some(AbilityTag::PowerUp) }` (mirror existing variant round-trip tests).
- **Coverage**: parser-only data path is inert until card-data regen+redeploy (`parser_fix_inert_until_data_regen`) — tests rehydrate real cards; the named cards flip to "supported" only after regen. Advancing the Spirit stays unsupported. Note this in the PR; do NOT claim in-game fixed-unreleased on parser-only evidence.
- **Verification**: `cargo fmt --all`; then `tilt logs clippy` / `tilt logs test-engine` (or `./scripts/tilt-wait.sh clippy test-engine`). Manually grep the parser diff for `.contains(`/`.starts_with(`/`.find(`/`.rfind(`/`.split(` (the parser-gate blind spots per `parser_gate_blind_spots`) — the new arms must be pure `tag()`/`alt()`/`take_until()`/`delimited()` composition (the `dispatch.rs:2186` style, never `split_once_on`/`scan_contains`).

---

## 4. Genuine Human Design Decisions to Resolve

1. **Wonder Man "additional time" scope (per-game vs per-turn).** Power-up's base limit is once-per-**game**. "Can be activated an additional time" with no qualifier is read as **per game** (cap 2 total). *Recommendation: per-game.* Low risk; worth a maintainer nod (if an official ruling says per-turn, the modeling would attach to a per-turn cap instead).
2. **`RestrictionExpiry::UntilEndOfNextTurnOf` arming idiom vs turn-number stamp.** The plan mirrors the existing `Duration::UntilEndOfNextTurnOf` convert-at-untap idiom (convert to `EndOfTurn` at the target turn's untap, pruned at its cleanup), with NO `bool` field. An alternative is stamping the target `turn_number` at lowering time. *Recommendation: the convert-at-untap mirror* — it reuses the codebase's existing, reviewed pattern and its cleanup prune; a turn-number stamp would mis-fire under extra-turn LIFO insertion (other extra turns shifting the number). Confirm preference.
3. **`UntilEndOfNextTurnOf` lowering split.** Splitting `Duration::UntilEndOfNextTurnOf { Controller }` away from `UntilPlayerNextTurn` (to the new mirror anchor) for restrictions is the correct fix for Kang. Confirm no other card relies on the old (start-of-next-turn) lowering for a *restriction* (the existing doc comment claims none does — Kang is the first).
4. **Coarse `CantActivateAbilities` player-status projection (B2).** `derived_views.rs:392` projects a power-up-only prohibition as the generic "can't activate abilities" status. Correctness is unaffected (the consult site is precise); only the displayed player-condition is coarse. *Recommendation: leave coarse for this wave* unless a finer status is desired.
5. **Skipped/prevented-extra-turn arming-leak edge (FIX 3).** If Kang's granted extra turn is skipped or prevented (a `turns_to_skip` decrement or a turn-start replacement `Prevented`) landing exactly on that turn, the untap-step conversion never runs and the `UntilEndOfNextTurnOf` restriction never expires (wrongly blocking later power-up abilities). The same edge exists in the analogous `Duration::UntilEndOfNextTurnOf` arming. *Recommendation: accept as a documented narrow edge for this wave* (comment at the conversion site + a recorded known-leak test anchor), pending a future shared robust-arming fix that would cover both `Duration` and `RestrictionExpiry`. Confirm acceptance.

All other axes are mechanical reuse of verified existing infrastructure.
