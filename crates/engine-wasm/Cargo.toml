[package]
name = "engine-wasm"
version.workspace = true
edition = "2021"
license.workspace = true

[package.metadata.wasm-pack.profile.release]
wasm-opt = false

[package.metadata.wasm-pack.profile.dev]
wasm-opt = false

[lib]
crate-type = ["cdylib"]

[dependencies]
engine = { path = "../engine", package = "phase-engine" }
phase-ai = { path = "../phase-ai" }
# LLM opponents (opt-in): prompt rendering, provider wire formats, response
# parsing. The `draft` feature is intentionally off here — draft prompts belong
# to `draft-wasm`, and enabling it would pull `draft-core` into this artifact.
phase-llm = { path = "../phase-llm" }
seat-reducer = { path = "../seat-reducer" }
rand = "0.9"
rand_chacha = "0.9"
wasm-bindgen = "0.2.114"
serde = "1"
serde_json = "1"
serde-wasm-bindgen = "0.6"
getrandom = { version = "0.3", features = ["wasm_js"] }
console_error_panic_hook = "0.1"
js-sys = "0.3"

[dev-dependencies]
# Unit tests place objects with the raw zone API, which is sealed in production
# builds; dev-dependencies never reach the shipped WASM artifact.
engine = { path = "../engine", package = "phase-engine", features = ["test-support"] }
